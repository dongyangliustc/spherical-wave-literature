# 2026-08-26 Memory

## 知识库草案 v0：自产知识与文献"同流不同信"体系

DY 提出设想：将多轮对话中的自产知识文档与既有文献知识库纳入同一条流水线作为输入，但为其内容真实性打上标签、逐步核实迭代。判断可行并产出设计草案。

- **结论**：可实现且已具备 80% 地基——`sources.yaml` 已是单管道多源（notes/ 深调、自有论文 paper-huang-2026-pdcs priority=1 均已同流）。缺口不在"同流"而在"同流但可信度不混淆"。
- **设计核心**：两个正交维度。维度 A 来源属性（T0 外部文献 / T1 可追溯派生 / T2 自产共识）；维度 B 核实状态（unverified → anchored → literature_supported/benchmark_verified/conflicting/unsupported，负向 refuted/superseded）。即把 metadata_schema 现有的 confidence/review_status 从"文献条目级"下沉到"主张/断言级"。
- **关键边界**：`claim_kind` 分轨——fact 强制文献锚定、derivation 推演可复现、hypothesis 永不注入代码上下文；黄金法则"未审查不入代码决策"不放松，只下沉到 chunk 级。门控放在检索层（epistemic gate）而非入库层。
- **落地清单**：sources.yaml 加 provenance 字段；chunk 表加列；新增 index/registry/claims.yaml；tools/capture_claims.py；registry_query/semantic 加门控；review packet 加"主张核实周报"节。
- **草案位置**：`docs/literature_harness/自产知识同流与核实迭代_草案v0.md`
- **待 DY 裁决**（草案 §8 六个决策点）：核实粒度、创新类分轨、T1 验证标准、默认召回/注入策略、命名空间（建议合并+打标）、实施分期（建议 P0+P1 两周内）。
- **7 澄清**：DY 对"核实粒度"不解、其余默认合理。用 Phase G 真实段落（①文献事实 ②实施说明 ③核心假设）画三档对比图解释。推荐"主张级为主 + chunk 继承"：精确状态落主张卡片（人工主张级审），chunk 检索时继承所含主张最差值（fail-closed），不放行未核实内容。待 DY 回复"1 同意"后落 v0.1。

## v0.1 实施完成 + 试点 3 闭环跑通（2026-08-26 下午）

HY 已回复"同意"，六决策点全部通过，落地 v0.1（P0+P1）：

- **核心落地**：sources.yaml 加 `provenance` 字段（literature/own_publication/synth_summary/ideation）+ own_note/claim_manifest 新 source_type；chunk 表加列（provenance/epistemic_status/claim_kind，`store.py` 自动迁移 + fail-closed 默认）；检索层 `min_epistemic_status` 门控（EPISTEMIC_RANK：benchmark_verified=3 > literature_supported=2 > consistent=1 > unverified=0 > conflicting=-1 > refuted/superseded/stale=-2，注入代码上下文硬门槛 ≥2）；`registry_query.query_claims` + `query_claims_tool`；`tools/capture_claims.py` 抽取+保守锚定；周报模板加「主张核实周报」节。
- **试点语料**：`notes/对话知识_核实试点_2026-08-26.md`（own_note，13 条 bullet 主张，第一节=系统设计共识无锚点，第二节=Phase G 技术判断可锚定文献）。
- **关键修复**：capture_claims 首次 dry-run 全部自锚定到 own-note chunk（自身字面命中 score 最高）→ 修复为排除 ideation provenance + 排除自身来源，保守三级（T0/T0* 命中且 score≥100 → literature_supported；非 ideation 弱锚 → consistent；无锚 → unverified）。修复后 0 条自动上浮。
- **索引与门控数字**：ingest 重建 2066 chunks（synth_summary 685/literature 1365/own_publication 13/ideation 3；consistent 685/literature_supported 1378/unverified 3；derivation 685/fact 1378/hypothesis 3）。LB94 查询：全量召回 own-note 排第 1 名，加 gate 后 ideation 召回=0。claims.yaml 15 卡（clm-0001 种子 + 捕获 14），clm-0002 无锚 unverified，12 条 consistent，clm-0013 经 DY 人工核实升级为 literature_supported（锚 Domcke 1983）→ query_claims gate 0→1 条。query_by_module('separable_potential') 合并 4 文献 + 1 主张。
- **回归**：pytest 8/8（修 2 个过期测试：server 工具集断言 3→7 个；test_sources Windows 路径跨平台断言）。
- **产物位置**：草案已升版 v0.1（同文件 `docs/literature_harness/自产知识同流与核实迭代_草案v0.md`，§8 决策已裁决、§9 已更新）；试点报告 `docs/literature_harness/自产知识同流_试点报告_v0.1.md`。
- **遗留**：12 条 consistent 待人工周报复核；clm-0001 hypothesis 进验证债务队列；MCP connector 需重启才能用新工具；consistent 弱锚点有漂移风险靠周报兜底。

## 草案重排为"科学汇报版"（2026-08-26 晚）

DY 要求把流程图放进草案并改成易汇报格式。已完成 `docs/literature_harness/自产知识同流与核实迭代_草案v0.md` 全文重排：

- **嵌入 4 张 Mermaid 图**（Typora/Obsidian/VS Code/GitHub 可渲染导出）：图1 双轴坐标系、图2 同流不同信流水线、图3 主张生命周期状态机、图4 检索门控判定。
- **汇报骨架**：摘要(Abstract) → 背景与动机(现状盘点+缺口 G1-G4 表) → 设计核心(双轴) → 系统架构 → 派生机制 → 技术落地清单(含状态列) → 试点结果(IC 表) → 决策记录(D1-D6 表) → 风险边界 → 下一步 + 附录文件索引。
- 内容无实质性变更，仅重排结构 + 图表化；所有已裁决决策与试点结论保留。交付前用 show_widget 预览了图2+图3 效果。
